# API Configuration
# Use http://localhost:8000/api for development
# Use your production API URL for production builds
VITE_API_URL=http://localhost:8000/api

# Environment
NODE_ENV=development

# App Info
VITE_APP_NAME=Flashcard App
VITE_APP_VERSION=1.0.0

# Note: When running with Docker Compose, the VITE_API_URL
# is set via docker-compose.yml environment variables
